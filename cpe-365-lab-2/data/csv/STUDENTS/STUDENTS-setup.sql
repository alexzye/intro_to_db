CREATE TABLE Teachers(Last VARCHAR(12) NOT NULL, First VARCHAR(12) NOT NULL, Classroom INT NOT NULL PRIMARY KEY);
CREATE TABLE Students(Last VARCHAR(15) NOT NULL, First VARCHAR(15) NOT NULL, Grade INT NOT NULL, Classroom INT NOT NULL, FOREIGN KEY(Classroom) REFERENCES Teacher(Classroom), UNIQUE(Last, First));
